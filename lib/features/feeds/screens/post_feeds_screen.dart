import 'package:flutter/material.dart';

class PostFeedsScreen extends StatelessWidget {
  const PostFeedsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("PostFeeds"),
      ),
    );
  }
}
